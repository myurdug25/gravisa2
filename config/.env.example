# Gravisa - Ortam değişkenleri
# cp config/.env.example config/.env
# .env dosyası config/ içinde olduğu için web'den erişilemez.
# Detaylı bilgi: DEPLOY.md

# Ortam: production | development
APP_ENV=production

# Hata gösterimi (production'da mutlaka 0)
APP_DEBUG=0

# Uygulama URL kökü (https zorunlu, örn: https://gravisa.com)
APP_URL=

# Saat dilimi (opsiyonel)
APP_TIMEZONE=Europe/Istanbul

# ---------- E-posta ----------
MAIL_TO=info@gravisa.com
MAIL_FROM_NAME=Gravisa Web Sitesi
MAIL_FROM=noreply@gravisa.com

# SMTP (boş bırakırsanız PHP mail() kullanılır)
MAIL_SMTP_HOST=
MAIL_SMTP_PORT=587
MAIL_SMTP_USER=
MAIL_SMTP_PASS=
MAIL_SMTP_SECURE=tls

# ---------- Admin ----------
ADMIN_USER=admin
ADMIN_PASS=

# Oturum süresi (saniye)
SESSION_LIFETIME=3600

# ---------- Veritabanı (kullanmak için USE_DB=1) ----------
USE_DB=0
DB_HOST=localhost
DB_NAME=gravisa
DB_USER=root
DB_PASS=
